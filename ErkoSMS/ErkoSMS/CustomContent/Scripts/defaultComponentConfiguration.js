

// Component Defaults
(function () {
    if (window.vex) {
        vex.defaultOptions.className = 'vex-theme-metro';

        // By default, don't close vex dialogs when clicked outside of dialog
        vex.defaultOptions.overlayClosesOnClick = false;
    }

})();
